
#  ////////////////////////////////////////////////////////////////////
#  //                P R O J E C T A R C H E T Y P E                 //
#  //                       www.SeboCorp.com                         //
#  ////////////////////////////////////////////////////////////////////

#  Rewrites Apache's URLs to be search-engine friendly cleaner looking
#  before they ever reach Archetype and helps secure the filesystem a bit.

#  Copyright © 2007 Sebo Corporation.  All rights reserved.

<IfModule mod_rewrite.c>
 # Enable Apache's Rewrite engine
   RewriteEngine On

 # Nobody should be peeking around our file tree
   RewriteCond %{REQUEST_FILENAME} system
   RewriteRule .* - [F,L]

 # Change all requests to appear as though they're static ( but only if they're not real )
   RewriteCond %{REQUEST_FILENAME} !-f
   RewriteCond %{REQUEST_FILENAME} !-d
   RewriteRule ^(.+)$ ./index.php?x=$1 [L]
</IfModule>
